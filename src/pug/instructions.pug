doctype html
html
    include includes/head
    body
        h1=config.title
        h2(style="text-align:center;") Instructions
        p(class="question")
            | You will be presented with a series of statements. For each one, click the button with your opinion on it. Now, choose a version of the text to take.
        a(href="quiz.html?r" class="no-under")
            button(
                class="button"
                id="longqr-button"
            )="Full Version (Shuffled) (" + longq + " Questions)"

        a(href="quiz.html?sr" class="no-under")
            button(
                class="button"
                id="shortqr-button"
            )="Short Version (Shuffled) (" + shortq + " Questions)"

        a(href="quiz.html" class="no-under")
            button(
                class="button"
                id="longq-button"
            )="Full Version (Ordered By Value) (" + longq + " Questions)"

        a(href="quiz.html?s" class="no-under")
            button(
                class="button"
                id="shortq-button"
            )="Short Version (Ordered By Value) (" + shortq + " Questions)"

        a(href="index.html" class="no-under")
            button(
                class="button"
                id="nevermind-button"
            ) Wait, nevermind!
